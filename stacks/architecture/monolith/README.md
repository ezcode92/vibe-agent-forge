# Monolith

## 목적

단일 배포 단위 안에서 단순한 경계를 유지하고 불필요한 분산을 방지하는 원칙을 담당합니다.

## 포함된 문서 자산

- Monolith 전용 `agents.fragment.md`
- 다른 내부 architecture 스타일과의 조합 기준

## 현재 범위

`agents.fragment.md`는 Monolith 지침 fragment로 존재하며 `registry/fragments.yml`에서 조회할 수 있습니다. 배포 설정과 애플리케이션 구현은 포함하지 않습니다.
