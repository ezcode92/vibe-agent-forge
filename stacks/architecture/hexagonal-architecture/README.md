# Hexagonal Architecture

## 목적

port와 adapter 경계를 통해 domain의 외부 의존성을 차단하는 원칙을 담당합니다.

## 포함 예정 파일

- Hexagonal Architecture 전용 `agents.fragment.md`
- port·adapter 책임과 의존성 검증 기준

## 현재 범위

실제 fragment, package template과 adapter 구현은 아직 작성하지 않습니다.
