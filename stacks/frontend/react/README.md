# React

## 목적

React의 component, hook, state 책임과 렌더링·접근성·테스트 원칙을 담당합니다.

## 포함된 문서 자산

- React 전용 `agents.fragment.md`
- backend API 조합에 필요한 bridge 참조 정보

## 현재 범위

`agents.fragment.md`는 React 지침 fragment로 존재하며 `registry/fragments.yml`에서 조회할 수 있습니다. Package 설정과 component 코드는 포함하지 않습니다.
