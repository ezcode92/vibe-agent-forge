# Spring–RDB Bridge

## 목적

Spring transaction·repository 경계와 RDB constraint·migration·query 원칙을 연결합니다.

## 포함 예정 파일

- Spring–RDB 전용 bridge `agents.fragment.md`
- transaction과 영속성 검증 기준

## 현재 범위

실제 bridge fragment, ORM 설정, schema와 migration은 아직 작성하지 않습니다.
