# bridge

## 목적

둘 이상의 stack을 함께 선택할 때만 필요한 interface, lifecycle과 책임 연결 규칙을 관리합니다.

## 포함된 문서 자산

- stack 조합별 bridge `agents.fragment.md`
- bridge 적용 조건과 필수 구성 요소 메타데이터

## 현재 범위

현재 bridge fragment 8개가 존재하며 `registry/fragments.yml`에서 dependency와 함께 조회할 수 있습니다. 병합 engine과 실행 로직은 포함하지 않습니다.
