# api

## 목적

기술 구현과 분리된 API 계약, protocol 의미와 호환성 원칙을 관리합니다.

## 포함된 문서 자산

- API 유형별 `agents.fragment.md`
- framework와 client 조합에 필요한 bridge 참조 정보

## 현재 범위

RESTful API fragment가 존재하며 `registry/fragments.yml`에서 조회할 수 있습니다. API schema, endpoint와 server 구현은 포함하지 않습니다.
