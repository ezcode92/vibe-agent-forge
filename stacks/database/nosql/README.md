# NoSQL

## 목적

접근 패턴 중심 데이터 모델, consistency, partition과 index 선택 원칙을 담당합니다.

## 포함된 문서 자산

- NoSQL 전용 `agents.fragment.md`
- 제품별 확장 지점을 설명하는 메타데이터

## 현재 범위

`agents.fragment.md`는 NoSQL 지침 fragment로 존재하며 `registry/fragments.yml`에서 조회할 수 있습니다. Schema와 특정 DB 제품 설정은 포함하지 않습니다.
